meta {
  name: Delete Product
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/product/:product_id
  body: none
  auth: inherit
}

params:path {
  product_id: {{product_id}}
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}

docs {
  DELETE /api/products/:product_id Delete product endpoint
}
