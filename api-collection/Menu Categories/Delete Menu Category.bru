meta {
  name: Delete Menu Category
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/menu-category/:category_id
  body: none
  auth: inherit
}

params:path {
  category_id: {{category_id}}
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}

docs {
  DELETE /api/menu-categories/:category_id Delete menu category endpoint
}
