meta {
  name: Create Menu Category
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/menu-categories
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}

docs {
  POST /api/menu-categories Create menu category endpoint
}
