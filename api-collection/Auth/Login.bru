meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "owner2@demo.com",
    "password": "Passw0rd"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  POST /api/auth/login/:rol Login endpoint Protected with rate limiting: 5 attempts per 15 minutes
}
