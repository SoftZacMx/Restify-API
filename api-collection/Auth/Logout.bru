meta {
  name: Logout
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/auth/logout
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}

docs {
  POST /api/auth/logout — Limpia la cookie HttpOnly con el JWT.
}
