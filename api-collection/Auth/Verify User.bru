meta {
  name: Verify User
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/verify-user
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "owner@demo.com"
  }
}

settings {
  encodeUrl: true
}

docs {
  POST /api/auth/verify-user Verify user endpoint Protected with rate limiting: 3 attempts per 15 minutes (prevents email enumeration)
}
