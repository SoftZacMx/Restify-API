meta {
  name: List Users
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  GET /api/users List users endpoint (with optional filters) NOTE: This must come before /:user_id route
}
