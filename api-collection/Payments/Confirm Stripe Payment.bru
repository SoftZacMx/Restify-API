meta {
  name: Confirm Stripe Payment
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/payment/stripe/confirm
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}

docs {
  POST /api/payment/stripe/confirm
}
