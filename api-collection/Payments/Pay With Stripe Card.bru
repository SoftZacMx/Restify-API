meta {
  name: Pay With Stripe Card
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/payments/card-stripe
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "orderId": "{{orderId}}",
    "connectionId": null
  }
}

settings {
  encodeUrl: true
}

docs {
  POST /api/payments/card-stripe — Inicia un pago con tarjeta vía Stripe para un pedido.
}
