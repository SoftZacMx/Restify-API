meta {
  name: List Payments
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/payments
  body: none
  auth: inherit
}

params:query {
  ~orderId:
  ~userId:
  ~status:
  ~paymentMethod:
  ~dateFrom:
  ~dateTo:
}

settings {
  encodeUrl: true
}

docs {
  GET /api/payments — Lista pagos. Filtros opcionales: orderId, userId, status, paymentMethod, dateFrom, dateTo.
}
