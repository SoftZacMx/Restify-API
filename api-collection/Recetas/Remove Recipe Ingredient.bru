meta {
  name: Remove Recipe Ingredient
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/menu-items/{{menuItemId}}/recipe/items/{{productId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Quita un ingrediente de la receta.
}
