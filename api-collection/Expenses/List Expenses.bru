meta {
  name: List Expenses
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/expenses
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{login-token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  GET /api/expense/
}
