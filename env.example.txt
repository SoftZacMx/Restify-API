# Copiar este archivo a .env.local y configurar los valores

# Database (usuario creado en scripts/mysql-init/01-setup-user.sql)
DATABASE_URL="mysql://restify_user:restify_password@localhost:3306/restify"

# Redis
REDIS_URL="redis://localhost:6379"

# AWS Configuration (LocalStack)
AWS_ENDPOINT_URL="http://localhost:4566"
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="test"
AWS_SECRET_ACCESS_KEY="test"

# Email (AWS SES) — apagable. EMAIL_ENABLED=false → no envía, solo loggea (dev/test sin credenciales)
EMAIL_ENABLED="false"
EMAIL_FROM="no-reply@restify.app"

# SQS Queues
PAYMENT_PROCESSING_QUEUE_URL="http://localhost:4566/000000000000/payment-processing-queue"
PAYMENT_NOTIFICATIONS_QUEUE_URL="http://localhost:4566/000000000000/payment-notifications-queue"

# EventBridge
EVENT_BUS_NAME="payment-events"

# DynamoDB
DYNAMODB_ENDPOINT="http://localhost:4566"
WEBSOCKET_CONNECTIONS_TABLE="websocket-connections"
PAYMENT_SESSIONS_TABLE="payment-sessions"

# Stripe (Test Mode)
STRIPE_SECRET_KEY="sk_test_your_key_here"
STRIPE_WEBHOOK_SECRET="whsec_test_webhook_secret"
STRIPE_PUBLISHABLE_KEY="pk_test_your_key_here"

# WebSocket (Local)
WEBSOCKET_API_ENDPOINT="http://localhost:4566"

# JWT
JWT_SECRET="your_jwt_secret_key_here"
JWT_EXPIRES_IN="24h"

# Environment
NODE_ENV="development"
IS_LOCAL="true"

# Application
APP_NAME="Restify API"
APP_VERSION="1.0.0"

# Crons / jobs programados (4.1.F)
# RUN_CRONS: "false" desactiva los crons en este proceso (usar en instancias
#   secundarias si algún día se escala horizontalmente; default "true").
RUN_CRONS="true"
# Días sin verificar email antes de cerrar la cuenta (default 7).
UNVERIFIED_RETENTION_DAYS="7"
# Expresión cron del job de limpieza (default "0 4 * * *" = 4:00 AM diario).
CLEANUP_UNVERIFIED_CRON="0 4 * * *"
# Timezone de los crons (default "America/Mexico_City").
CRON_TIMEZONE="America/Mexico_City"

